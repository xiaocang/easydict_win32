# Easydict 全窗口 Fluent 2 原生 UI 重构

本计划以用户最新要求为准：页面 UI 不使用 React 或 Web 技术，以 C#、XAML 和 WinUI 3 原生控件
实现本文规定的 Fluent 2 视觉与交互效果；富词典内容允许使用 WebView2，作为明确例外。
原生实现必须达到布局、材质层级、排版、
选中态和操作反馈要求；仅替换控件或采用默认样式不构成完成。
四张参考图用于逐项视觉对照；分类、Mini 行为和数据规则遵循本文已确认的约定。
此前的 React、WebView2 Island、Web Bridge 和 Mini 内嵌保存项面板方案均已废止。
此前实施及测试证据记录在 [FLUENT2_UI_ACCEPTANCE.md](FLUENT2_UI_ACCEPTANCE.md)，
本次原生页面、富词典例外与视觉达标要求的待办、完成条件以本文为准。

## 当前状态（2026-09-05）

全窗口原生重构已落地，本轮可执行的运行验收已完成；旧保存项验收与本轮证据分别记录，未覆盖项不标为通过。

设置页补齐显式应用主题初始化、独立浅深色资源及文字前景色绑定。现有 WinUI 主题矩阵已验证系统与应用浅／深色的四种组合，并补测中文设置页在系统浅色、应用深色下的宽窄布局及系统主题动态切换；证据见验收记录。

- Main 使用一份输入、语言与结果控件，在 960 DIP 处切换左右／上下布局，并保存各布局滚动偏移。
- Main、Mini、Fixed 共用原生语言栏、输入表面、结果呈现与消息反馈；各自保留查询和窗口状态。
- 长文档已重排常驻参数、输出设置与更多选项；设置已接入宽屏 192 DIP 分类栏／窄屏原有标签、统一设置行和系统高对比资源。
- 富词典保留正文 WebView2，新增动态高度、字体及高对比同步、渲染代次校验和原生回退反馈。
- 新图标已用于本轮实际窗口截图；未替换已批准截图基线。
- 本轮两组单元测试分别通过 162 和 81 项，去重后共 237 项。UI 与视觉覆盖见 [本轮验收记录](FLUENT2_UI_ACCEPTANCE.md)。
- 本轮实际 100% 与 150% 缩放通过相关运行验收，150% 跨窗口 7 项通过。当前显示设置仅提供 100/125/150/175%，真实 200% 留待支持该缩放的环境；系统已恢复本轮开始时的 100%。
- 富词典参与 12 次导航／详情／对比与主题切换后，ActiveResults=3、AliveResults=3；已整理 11 张新截图候选，未替换批准基线。

## 范围

- 重构 Main 翻译与长文档、历史、收藏、查询详情、Mini、Fixed 和设置。
- 主翻译页整体重构；Mini 的标题栏和紧凑控件区通过更多菜单打开主窗口。
- 历史分类为全部、翻译 / 词典、语法纠错、OCR。
- Main 保留顶部翻译／长文档模式菜单；设置保留七个分类与内部标识。
- 使用新透明应用图标，标题栏、任务栏、系统托盘与打包资源保持一致。

### 技术边界：原生页面，富词典可用 WebView2

- 页面、导航、列表、详情、弹出菜单、编辑与反馈使用 C# / XAML / WinUI 3。
- 页面布局、导航、列表、卡片外壳和操作区不使用 React、React DOM、Fluent UI React、HTML/CSS/JavaScript 页面、WebView2 或 Web Bridge 实现。
- 复用原生控件的键盘、焦点、辅助功能和虚拟化能力，通过主题资源、样式、必要的 ControlTemplate 与自定义原生 Panel 达到视觉规范。
- 数据查询、SQLite、Provider 请求和剪贴板等继续使用现有 C# 服务；原生页面要求不影响联网翻译或数据中包含 HTML 文本。
- 富词典内容允许沿用 WebView2 呈现 HTML、样式及词典资源。例外仅限结果卡内部的词典内容，不扩展为整页或其他结果的 Web UI。
- 富词典的 Provider 标题、耗时、折叠、复制、播放、收藏、更多和消息反馈仍由原生控件及 C# 宿主管理。
- WebView2 例外仅限富词典正文，所有窗口的外壳、操作和反馈保持原生。

### 应用图标

最新图稿为用户提供的 `e3b344d0-f932-4bd0-a5d1-dc908ecc998e.png`，
取代上一版书本与彩色书签图标。原图自带透明通道，外围不得填充黑色或其他实体底色。
仓库原图为 [AppIconSource.png](dotnet/src/Easydict.WinUI/Assets/Branding/AppIconSource.png)。

- 复用 `generate-windows-assets.ps1` 生成缩放、targetsize 和 unplated 资源。
- 复用 `generate-app-icon-ico.ps1` 生成 16、24、32、48、64、128、256 像素 ICO；构建时同步生成 32 像素托盘 PNG。
- 标题栏和任务栏使用同源嵌入 ICO，托盘使用 ICO 或透明 PNG 回退；打包图标与启动图同步更新。
- 标题栏图标跟随应用主题，托盘图标独立跟随 Windows 任务栏主题。深色资源由同一张 AppIconSource.png 预生成，包含 16–256 像素 PNG、多尺寸 ICO 和托盘 PNG，保留完整构图、品牌色和外围透明。运行时只切换资源文件，不再转换或绘制 HICON；主题切换即时更新，替换的托盘图标及时释放。
- 已检查 42 个顶层 PNG 和 ICO 的 7 个尺寸透明边角；构建输出托盘 PNG 保持透明，输出 ICO 与仓库 ICO 一致。

## 全窗口布局与共享组件（本轮执行约定）

- Main 页面宽度 ≥960 DIP：左侧 360 DIP 输入、语言和查询操作，右侧自适应结果，间距 16。双栏时左侧内容能完整放入视口则固定显示，右侧结果独立滚动；左侧超出高度时仅在左栏内滚动，禁止边界滚动传递到外层。较窄时上下排列并统一滚动；最大内容宽度 1280，左对齐；超出最大宽度的留白放在右侧。重排复用控件，保留输入、语言、查询、结果与各布局滚动偏移。
- 页面宽／窄边距 24／16；浮窗标准／紧凑边距 12／8；区块间距 16／8。标题 24、区块 16、正文 14、元信息 12；结果正文 14 × FontScale，范围 0.85–1.4。按钮至少 32，卡片圆角 8。
- 共享原生输入表面、语言栏布局、结果卡与消息反馈。各宿主独立维护语言路由、服务、查询与取消令牌。
- 所有结果卡标题常驻复制（包括折叠），播放、收藏等按已有窗口能力呈现于稳定操作区。浮窗不新增单结果收藏。
- 翻译结果标题栏保留重构前字号：Provider 名称基准 12 DIP、耗时／状态基准 10 DIP，沿用原有 FontScale 行为；标准、Minimal 及保存项详情均遵循此例外，不套用页面标题或元信息的新字号。
- Mini 保留摘要编辑、聚焦全选、置顶与失焦隐藏以及更多菜单保存项入口；Fixed 保留多行输入、独立尺寸、不随失焦隐藏，不增加保存项导航。隐藏保留草稿及结果，真正关闭才释放。
- 长文档按文件队列、翻译参数、输出设置、进度与结果组织。语言、Provider、输出方式常驻；输入识别、并发与页码在更多选项。保持模式切换状态和独立任务历史。
- 设置 ≥960 DIP 使用固定的左侧 192 DIP 分类栏、右侧独立滚动内容；左栏超高时可单独滚动，右侧滚动不会带动分类和页头。较窄恢复原有图标加文字标签，横向排列并按空间换行。页面、标签栏及内容左对齐，超过最大内容宽度后不居中。保留七个分类、即时保存、校验、懒加载与快捷键冲突处理，不增加设置搜索。
- 富词典主题、高对比和字体随宿主同步；ResizeObserver 通过现有消息通道报告动态高度；初始化、导航与尺寸消息校验渲染代次。折叠按需加载，失败保留可读文字和原生提示。
- 本轮顺序：公共组件 → Main → Mini／Fixed → 长文档 → 设置 → 保存项整合与验收。保留业务和数据规则，不发布 MSIX、不自动提交 Git、不自动替换批准截图基线。
- 验收覆盖四主题、两密度、实际 100／150／200% DPI，400／640／960／1280 DIP 与浮窗最小宽度。富词典固定 HTML（延迟图片、长正文、字号、折叠复制、回退及释放），保存项 200／5000 条分页、虚拟化与生命周期。功能、技术边界和视觉证据分别记录于验收报告。
## 数据规则

复用 `SavedItemsService`、`SavedItemsStore`、查询快照、收藏和清理服务。
SQLite 文件仍为用户设置目录的 `saved_items.db`，schema 版本保持 1。
收藏成功结果数量通过查询投影获取，不增加列、不迁移数据库。

只有成功且非空的结果进入快照。部分成功只保存成功结果；全部失败不保存；
取消后已有成功结果仍然保存。Main、Mini、Fixed、OCR 使用相同记录服务。
长文档不进入历史和收藏。

保存查询和结果使用事务。收藏引用整次查询或其中一个结果；同一查询的兄弟结果
在收藏存续期间保持可访问。删除历史仅改变可见性，清理不能破坏收藏的查询和结果。
移除最后一个收藏时，隐藏或已经过期且没有其他收藏的查询可由现有清理逻辑回收。

### 列表预览

- 原文摘要单行；Provider、语言、类型、成功结果数量、时间使用 12 DIP 元信息。
- 结果正文 14 DIP，自然换行，卡片随内容增高。
- 压缩连续空白；按用户可见字符（grapheme/text element）计算长度。
- 不超过 100 字符完整显示；超出时保留前 100 字符并追加省略号。
- 不通过 MaxLines、固定高度或再次省略截断结果预览。
- 翻译用译文，词典优先首个主要释义，语法用纠正文本，OCR 用翻译正文。
- 预览优先级：指定 Provider → 命中搜索的成功 Provider → 默认成功 Provider。
- 单结果收藏始终展示被收藏的 Provider；整次查询收藏按查询预览规则处理。

## 原生页面与视觉规范

页面使用 `NavigationView`、虚拟化 `ListView`、`SelectorBar`、`ComboBox`、
`CommandBar`、`Expander`、`InfoBar` 及原生结果视图。
富词典内容可沿用现有 WebView2 渲染及按需加载策略，外层卡片与操作保持原生。

| 项目 | 规格（DIP） |
| --- | --- |
| 宽页面 | 56 导航栏 + 360 列表 + 自适应详情 |
| 响应式 | 页面宽度 ≥960 并排；不足 960 逐页切换；不足 640 导航收为菜单 |
| 外边距 | 宽页面 24，窄页面 16 |
| 间距 | 区块 16，列表卡片 6 |
| 卡片 | 列表内边距 12，详情 16，圆角 8，细描边，无常驻阴影 |
| 字号 | 页面标题 24，区块标题 16，正文 14，元信息 12 |
| 控件 | 常规操作高 32，搜索框 36，导航项约 40 |

Mica 仅用于窗口背景；内容使用主题感知实体表面。卡片有独立的普通、悬停、
强调色浅底选中和键盘焦点状态，兼容 Light、Dark、Minimal 和高对比。
详情正文支持用户字体缩放。标签、元信息和按钮不横向溢出。

### Fluent 2 效果与原生实现要求

| 视觉与交互目标 | 原生实现方式 | 验收要求 |
| --- | --- | --- |
| 浅层背景与清晰内容层级 | 窗口 Mica、主题感知页面底色和实体卡片；统一 ThemeResource | 内容不受背景透色干扰；Light/Dark 层级清楚，高对比使用系统颜色 |
| 紧凑、有节奏的列表 | 统一 24/16/14/12 字号、间距与卡片模板；原生文本换行 | 与上表尺寸一致，标题、摘要、元信息易于区分，无多余大块留白或二次裁切 |
| 蓝色选中态 | Light/Dark 使用品牌强调色浅填充和侧标，独立悬停、按下与焦点状态 | 不以禁用态表示当前项；高对比使用系统选中色；焦点不依赖选中底色 |
| 清楚且稳定的操作层级 | CommandBar 溢出、标题区复制、固定顺序的结果操作 | 窄屏仍可访问全部操作，折叠后复制不消失，不因内容变化错位 |
| 列表／详情与真正的双结果对比 | Grid 和原生 Panel 在断点处切换 | 宽屏列宽、窄屏返回、对比并排/堆叠与选择保持均按本文要求 |
| 分类与 Provider 单选 | 原生 SelectorBar 或 ComboBox，按可用宽度切换 | 原生下划线与图稿胶囊形态可有差别，但层级、选中辨识度、间距不能降低 |
| 轻量状态反馈 | 原生 VisualState、InfoBar 和适度过渡 | 悬停、按下、展开与复制反馈清晰；过渡遵循系统动画设置，不阻塞操作 |
| 图标一致且透明 | 同源多尺寸 PNG/ICO、原生图标控件 | 标题栏、任务栏、托盘无黑色矩形底，新图标不被拉伸或切边 |

Light/Dark 为 Fluent 2 主视觉验收主题。Minimal 保留用户选择的线框表现，但共享布局、
信息层级、操作和响应式规则；高对比按系统配色验收。以上明确差异以外的视觉偏差，
必须逐项记录并修正，不能仅以「使用原生控件」作为保留理由。

### 富词典 WebView2 例外

- 结构化释义可继续使用现有原生控件；包含富 HTML 排版或词典资源的结果可使用现有 WebView2 路径，不强制转换为原生内容模型。
- WebView2 限定在词典正文区域，卡片边框、标题、选中态、折叠与操作区遵循统一的原生 Fluent 2 样式。
- 沿用 HTML、图片、链接及词典资源的处理能力，适配所在页面的主题与用户字体缩放，避免正文区域与外层卡片出现突兀的底色或尺寸差异。
- 保留按需创建、主题刷新、异步渲染过期保护与释放机制；折叠时标题区复制始终可用。
- 渲染失败使用现有可读文本回退及明确反馈；使用真实富词典快照检查内容、窄屏、滚动、主题切换及反复打开后的资源回收。

### 历史列表

搜索和分类栏固定，列表与详情独立滚动。分类栏按实际标签所需宽度切换下拉框。
保留 150 ms 防抖、相关性排序、25 条一页的游标、请求取消及过期响应保护。

非搜索状态按本地日期分为今天、昨天、最近 7 天、更早；最近 7 天排除今天和昨天。
跨页追加合并同一分组，午夜更新分组；搜索期间关闭日期分组。
历史只包含成功结果，不设置「仅显示成功」筛选，不重复显示成功徽标。

分别提供暂无记录、无搜索结果、尚未选择和加载失败状态。加载失败可重试，
无搜索结果可清除搜索和筛选。不会用空的原文卡代替未选择状态。

### 查询详情与对比

原文卡显示全文、语言、类型、来源和时间，提供复制原文、收藏整次查询、
重新翻译及更多操作。操作区应能动态溢出。

默认展示全部成功结果；少量 Provider 使用单选栏，放不下时使用下拉框。
对比最多选择两个成功 Provider，进入时默认前两个；详情可用宽度 ≥640 两列等宽，
不足时上下排列，改变宽度时保留选择。普通浏览结果占满详情宽度。

标准和 Minimal 结果卡均在标题区保留复制按钮，包括折叠状态；播放、收藏、
更多操作放在稳定操作区。复制调用现有 C# 剪贴板服务；成功显示约 3 秒页内消息
并向辅助技术播报，失败显示可关闭错误。

返回列表恢复选中项、滚动位置与键盘焦点。只有重新翻译才把保存的原文、语言、
模式带回主翻译页。

### 收藏

与历史共用列表模板；星标表示收藏、图钉表示置顶。列表展示标签，编辑操作在详情。
历史详情收藏当前未过期查询时原地更新星标，保留结果控件、焦点和对比选择；异步操作固定查询 ID，并丢弃过期的详情状态响应。过期查询取消最后一个收藏时仍刷新列表，遵循原有清理规则。
整次查询收藏默认展示全部成功结果；单结果收藏先显示目标结果，同次查询其他结果
放入带数量的折叠区，切换条目后恢复折叠，展开时再创建结果控件。

备注和标签默认显示摘要，显式进入编辑；保存、取消均可用。
离开未保存条目（切换记录、页面、搜索或筛选）提供保存、放弃、取消切换。

### 导航与 Mini

导航统一为快速翻译、历史记录、收藏、设置；使用选中态，保留明显的「返回翻译」。
Main 和 Mini 共用 `App.OpenSavedItems(SavedItemsSection)`，恢复、激活主窗口。
已在保存项页面时原地切换，重复入口不叠加导航记录。
历史与收藏分别保存搜索、筛选、选择和位置。

Mini 保留输入、语言、结果及原有置顶／失焦隐藏规则。标准标题栏和紧凑控件区
都提供更多入口。Fixed 保持没有保存项导航入口的边界。

### 历史设置

历史默认开启，默认保留 30 天；范围 1–3650，步进 1／30。
关闭后停止新增并保留旧历史；收藏不受期限影响；长文档不进入历史和收藏。
期限变更、清理完成和错误都给出反馈；所有新增文本进入本地化资源。

## 生命周期与接口

- 页面使用 `ShowSectionAsync` 原地切换并恢复各分区状态。
- 使用 `IServiceResultView`、`ServiceResultViewHost`、`IsSavedItemView`。
- 结果视图复制完成事件由宿主订阅，释放时对称解绑。
- 离开页面取消请求、停定时器、释放结果控件及富词典资源；返回时按状态恢复。
- 主题变更刷新原生表面，必要时重建标准／Minimal renderer，保留用户选择及草稿。
- 分页预取延后到下一次 UI 调度，避免在原生列表布局过程中修改 ItemsSource。
- 搜索高亮颜色按页面缓存，在重新加载或主题变化时刷新；未命中的文本不创建高亮。
- 保留现有 AutomationId，增加覆盖新操作的自动化检查。

## 实施与验收

### 前一轮已完成的基础工作

- [x] 第一批：公共资源、原生导航、分类、卡片状态、响应式布局及操作区。
- [x] 第二批：列表元信息、空状态、Provider 选择、收藏编辑、标准／Minimal 结果卡。
- [x] 第三批：日期、搜索优先级、复制消息、状态恢复、设置与本地化、Mini 和文档。
- [x] 后续图标更新：采用最新用户图稿，保留透明度并生成各入口资源。

### 本次原生页面与视觉达标待办

- [x] 盘点页面及共享结果渲染链：WebView2 创建点仅位于 `ServiceResultItem` 词典正文，页面外壳均为原生。
- [x] 接入公共资源与布局组件，完成 Main、Mini／Fixed、长文档、设置及保存项整合。
- [x] 富词典动态高度、三种字号、折叠复制、初始化失败的文字回退与提示通过运行测试。
- [x] 修正设置语言排序与默认查询模式引起的误报修改；真实修改仍保留保存保护。
- [x] 在验收记录中分开记录功能、技术边界、视觉差异及未覆盖项。
- [ ] 使用新图标重新捕获 Light/Dark 主视觉，以及 Minimal、高对比和窄屏截图；补充标题栏、任务栏与托盘验证。
- [x] 补充原生页面边界检查、富词典内容回归和视觉对照记录；未覆盖的真实 200%、完整 Provider 文档作业及任务栏／托盘表面检查单独保留。

使用 WinUI 单元测试、FlaUI 页面测试以及实际窗口截图验收，编译通过不代表视觉完成。
覆盖 Light、Dark、Minimal、高对比，标准／紧凑密度，100／150／200% DPI，
400／640／960／1280 DIP，中文、emoji、组合字符、长词和长标签。
使用固定 200／5000 条数据检查分页、虚拟化、滚动；反复切换详情、页面和主题，
验证控件及订阅不持续增长。记录缺失覆盖，不把缺失截图当成通过。

本次完成条件还包括：

- 页面布局与操作区不加载 React/Web UI；WebView2 仅用于富词典正文。通过渲染链检查及包含富词典快照的运行测试验证此边界，不以 WebView2 实例数为零作为验收要求。
- 在相同主题、DPI、页面宽度和内容状态下对照图稿；按布局、材质、字号、间距、图标、选中态、操作区分别记录通过或差异。
- 布局尺寸按本文 DIP 规格验收；系统字体栅格化差异不要求逐像素相同，但裁切、溢出、错位和明显层级偏差必须修复。
- 功能测试、原生技术边界与视觉验收分别记录，三者通过才完成；旧的 134 项单元测试、19 次 UI 用例和截图候选不能自动替代新增验收。

### 前一轮验证记录

| 项目 | 结果 |
| --- | --- |
| UI 重构构建 | 未打包 WinUI 构建 0 警告、0 错误 |
| 新图标构建 | 独立目录构建成功，0 错误；存在原有 LocalAi `TranslateHandler.cs` 的 CS8601 警告 |
| DPI 与宽度 | 100%、150%、200% 下分别通过 Light/Dark/Minimal 用例；各测 400、640、960、1280 DIP，实际页面宽度误差小于 0.5 DIP |
| 密度与高对比 | 100%/200% 标准、150% 紧凑；另测 200% 紧凑交互及真实 Windows 高对比的两种密度 |
| 列表与详情 | 200/5000 条数据分页和虚拟化；真实双列对比、折叠复制、收藏编辑与懒加载通过 |
| 状态恢复 | 搜索、分类、选中项和返回重入通过；Mini 两种模式恢复 Main 并保留草稿 |
| 生命周期 | 12 次导航/详情/对比循环及三种主题切换后，活动与存活结果控件均为 3，已实现列表容器为 20 |

具体用例与覆盖边界见 [验收记录](FLUENT2_UI_ACCEPTANCE.md)，
截图见 [候选索引](artifacts/ui-screenshots/fluent2/baseline-candidates/README.md)。
系统缩放已恢复原来的 200%，高对比已恢复关闭。

### 后续验收与覆盖限制

- 本轮运行截图已使用新图标；旧 16 张保存项候选保留为历史记录，本轮候选另行存放。
- 现有 16 张候选经过人工逐张查看，尚未进行像素差异阈值验收或替换已批准基线。
- 已验证上述主题、密度和 DPI 组合，未穷举所有系统高对比配色与用户正文字号。
- 15 个语言资源文件已补齐新键；简体中文已翻译，其他新文案仍有英文回退。
- 本轮不含 MSIX 打包、发布及长时间压力运行。
