<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Easydict.WinUI.Views.Controls.MinimalServiceResultItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border
        x:Name="RootBorder"
        Background="{ThemeResource EasydictCardBackgroundBrush}"
        BorderBrush="{ThemeResource EasydictCardBorderBrush}"
        BorderThickness="{ThemeResource EasydictCardBorderThickness}"
        CornerRadius="{ThemeResource EasydictCardCornerRadius}"
        Margin="0,0,0,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition MinHeight="{ThemeResource EasydictResultHeaderMinHeight}"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border
                x:Name="HeaderBar"
                Grid.Row="0"
                Background="{ThemeResource EasydictResultHeaderBackgroundBrush}"
                BorderBrush="{ThemeResource EasydictCardBorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="10,0"
                PointerPressed="OnHeaderPointerPressed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        x:Name="ServiceNameText"
                        Grid.Column="0"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource EasydictPrimaryTextBrush}"
                        TextTrimming="CharacterEllipsis"
                        VerticalAlignment="Center"/>

                    <TextBlock
                        x:Name="StatusText"
                        Grid.Column="1"
                        FontSize="11"
                        Foreground="{ThemeResource EasydictSecondaryTextBrush}"
                        Margin="8,0,0,0"
                        Visibility="Collapsed"
                        VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <Border
                x:Name="ContentArea"
                Grid.Row="1"
                Padding="12,8"
                Visibility="Collapsed">
                <StackPanel Spacing="4">
                    <TextBlock
                        x:Name="PendingQueryText"
                        FontSize="12"
                        Foreground="{ThemeResource EasydictTertiaryTextBrush}"
                        Visibility="Collapsed"/>

                    <TextBlock
                        x:Name="ResultText"
                        TextWrapping="Wrap"
                        IsTextSelectionEnabled="True"
                        Foreground="{ThemeResource EasydictPrimaryTextBrush}"
                        FontSize="13"
                        Visibility="Collapsed"/>

                    <TextBlock
                        x:Name="ErrorText"
                        TextWrapping="Wrap"
                        IsTextSelectionEnabled="True"
                        Foreground="{ThemeResource EasydictCriticalBrush}"
                        FontSize="12"
                        Visibility="Collapsed"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
