<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Easydict.WinUI.Views.MiniWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Easydict.WinUI.Views.Controls"
    mc:Ignorable="d"
    Title="Easydict ᵇᵉᵗᵃ Mini">

    <!-- Background bound to a non-themed transparent brush so the window-level Mica
         backdrop applied in MiniWindow.xaml.cs is actually visible. -->
    <Grid Background="Transparent"
          Padding="0">
        <Border
            x:Name="WindowSurface"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            BorderBrush="{ThemeResource MainBorderBrush}"
            BorderThickness="0"
            CornerRadius="0"
            Padding="{ThemeResource FloatingWindowContentPadding}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header with Pin and Close buttons - also serves as drag region -->
                <Grid x:Name="TitleBarRegion"
                Grid.Row="0"
                Margin="0,0,0,4"
                Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ToggleButton
                x:Name="PinButton"
                Grid.Column="0"
                Click="OnPinClicked"
                ToolTipService.ToolTip="Pin window (stay on top)"
                AutomationProperties.Name="Pin window (stay on top)"
                Background="Transparent"
                BorderThickness="0"
                CornerRadius="{ThemeResource EasydictControlCornerRadius}"
                Width="28"
                Height="28"
                Padding="0">
                <FontIcon x:Name="PinIcon"
                        Glyph="&#xE718;"
                        FontSize="14"
                        Foreground="{ThemeResource ButtonForeground}"/>
            </ToggleButton>

            <TextBlock
                x:Name="TitleText"
                Grid.Column="1"
                Text="Quick Translate"
                FontSize="12"
                FontWeight="SemiBold"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>

            <Button
                x:Name="OcrButton"
                Grid.Column="2"
                Click="OnOcrClicked"
                Style="{StaticResource EasydictIconButtonStyle}"
                ToolTipService.ToolTip="OCR translate"
                AutomationProperties.Name="OCR translate"
                Width="28"
                Height="28"
                Padding="0">
                <FontIcon Glyph="&#xE722;"
                        FontSize="14"
                        Foreground="{ThemeResource ButtonForeground}"/>
            </Button>

            <Button
                x:Name="CloseButton"
                AutomationProperties.AutomationId="MiniWindowCloseButton"
                Grid.Column="3"
                Click="OnCloseClicked"
                Style="{StaticResource EasydictIconButtonStyle}"
                ToolTipService.ToolTip="Close"
                Width="28"
                Height="28"
                Padding="0">
                <FontIcon Glyph="&#xE8BB;"
                        FontSize="14"
                        Foreground="{ThemeResource ButtonForeground}"/>
            </Button>
        </Grid>

        <!-- Compact Input -->
        <Border x:Name="SourceTextContainer"
                Grid.Row="1"
                Background="{ThemeResource TextControlBackground}"
                BorderBrush="{ThemeResource TextControlBorderBrush}"
                BorderThickness="{ThemeResource EasydictCardBorderThickness}"
                CornerRadius="{ThemeResource FloatingInputCornerRadius}"
                Padding="{ThemeResource FloatingInputPadding}"
                Margin="{ThemeResource FloatingInputMargin}"
                PointerEntered="OnSourceTextContainerPointerEntered"
                PointerExited="OnSourceTextContainerPointerExited"
                Tapped="OnSourceTextContainerTapped">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <!-- Collapsed State TextBlock -->
                    <TextBlock x:Name="SourceTextCollapsed"
                               Text="{Binding Text, ElementName=InputTextBox, Mode=OneWay}"
                               MaxLines="1"
                               TextTrimming="CharacterEllipsis"
                               IsTextSelectionEnabled="False"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                               VerticalAlignment="Center"
                               Visibility="Visible" />

                    <!-- Expanded State TextBox (Original) -->
                    <TextBox
                        x:Name="InputTextBox"
                        PlaceholderText="Enter text..."
                        BorderThickness="0"
                        Background="Transparent"
                        MinHeight="40"
                        MaxHeight="120"
                        AcceptsReturn="True"
                        TextWrapping="Wrap"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        PreviewKeyDown="OnInputKeyDown"
                        LostFocus="OnInputTextBoxLostFocus"
                        Visibility="Collapsed">
                        <TextBox.Resources>
                            <SolidColorBrush x:Key="TextControlBackground" Color="Transparent" />
                            <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="Transparent" />
                            <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="Transparent" />
                            <SolidColorBrush x:Key="TextControlBorderBrush" Color="Transparent" />
                            <SolidColorBrush x:Key="TextControlBorderBrushPointerOver" Color="Transparent" />
                            <SolidColorBrush x:Key="TextControlBorderBrushFocused" Color="Transparent" />
                            <Thickness x:Key="TextControlBorderThemeThickness">0</Thickness>
                            <Thickness x:Key="TextControlBorderThemeThicknessFocused">0</Thickness>
                        </TextBox.Resources>
                    </TextBox>
                </Grid>

                <Button x:Name="SourcePlayButton"
                        Grid.Column="1"
                        Click="OnSourcePlayClicked"
                        Style="{StaticResource EasydictIconButtonStyle}"
                        ToolTipService.ToolTip="Play source text"
                        Width="28"
                        Height="28"
                        Padding="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="4,0,0,0">
                    <FontIcon x:Name="SourcePlayIcon"
                               Glyph="&#xE768;"
                               FontSize="14"
                               Foreground="{ThemeResource ButtonForeground}" />
                </Button>
            </Grid>
        </Border>

        <!-- Mode + Language Bar -->
        <Grid Grid.Row="2"
                Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- Source Language -->
                <ColumnDefinition Width="Auto"/>
                <!-- Swap Button -->
                <ColumnDefinition Width="*"/>
                <!-- Target Language -->
                <ColumnDefinition Width="Auto"/>
                <!-- Translate Button -->
            </Grid.ColumnDefinitions>

            <ComboBox
                x:Name="SourceLangCombo"
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                SelectionChanged="OnSourceLangChanged"
                ToolTipService.ToolTip="Source Language">
                <!-- Populated dynamically in code-behind -->
            </ComboBox>

            <Button
                x:Name="SwapButton"
                Grid.Column="1"
                Click="OnSwapClicked"
                Width="28"
                Height="28"
                Padding="0"
                Style="{StaticResource EasydictIconButtonStyle}"
                ToolTipService.ToolTip="Swap languages"
                Margin="4,0">
                <FontIcon Glyph="&#xE8AB;"
                        FontSize="12"
                        Foreground="{ThemeResource ButtonForeground}"/>
            </Button>

            <ComboBox
                x:Name="TargetLangCombo"
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                SelectionChanged="OnTargetLangChanged"
                ToolTipService.ToolTip="Target Language">
                <!-- Populated dynamically in code-behind -->
            </ComboBox>

            <Button
                x:Name="TranslateButton"
                Grid.Column="3"
                Click="OnTranslateClicked"
                Width="32"
                Height="32"
                Padding="0"
                Style="{StaticResource EasydictPrimaryRoundButtonStyle}"
                ToolTipService.ToolTip="Translate"
                Margin="4,0,0,0">
                <Grid>
                    <ProgressRing
                        x:Name="LoadingRing"
                        IsActive="False"
                        Width="14"
                        Height="14"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                        Visibility="Collapsed"/>
                    <FontIcon
                        x:Name="TranslateIcon"
                        Glyph="&#xE8C1;"
                        FontSize="14"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                </Grid>
            </Button>
        </Grid>

        <!-- Detected Language Label -->
        <TextBlock
            x:Name="DetectedLangText"
            Grid.Row="3"
            Text=""
            FontSize="10"
            Foreground="{ThemeResource BlueAccentBrush}"
            Margin="0,0,0,2"
            Visibility="Collapsed"/>

        <!-- Multi-Service Results -->
        <ScrollViewer
            x:Name="MainScrollViewer"
            Grid.Row="4"
            VerticalScrollBarVisibility="Auto"
            ViewChanged="OnMainScrollViewerViewChanged">
            <ItemsControl x:Name="ResultsPanel">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <TextBlock
            x:Name="StatusText"
            Grid.Row="5"
            Text=""
            FontSize="10"
            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
            HorizontalAlignment="Right"
            Margin="0,4,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
